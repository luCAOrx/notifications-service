import { ApiOperationOptions } from '@nestjs/swagger';

export const apiOperationOptions: ApiOperationOptions = {
  summary: 'Send notification',
  description: 'That route is responsible for send notification',
};
