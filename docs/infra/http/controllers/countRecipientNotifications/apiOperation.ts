import { ApiOperationOptions } from '@nestjs/swagger';

export const apiOperationOptions: ApiOperationOptions = {
  summary: 'Count recipient notifications',
  description: 'That route is responsible for count recipient notifications',
};
